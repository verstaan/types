name: sherlock test
on: [push]
jobs:
  check-categories:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: python test
      - uses: actions/setup-python@v2
      - run: sh py_sher_test.sh
